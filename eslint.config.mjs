// @ts-check
import withNuxt from './.nuxt/eslint.config.mjs'

// Provide custom ESLint flat configuration via withNuxt.
// Do NOT pass a nested `config` key (ESLint will reject unknown options).
// Add rules or overrides directly in this object if needed.
export default withNuxt({
  // Example: add rules here (flat config format)
  // rules: {
  //   'no-console': 'warn'
  // }
})
